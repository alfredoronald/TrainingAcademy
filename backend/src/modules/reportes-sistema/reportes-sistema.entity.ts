import { Entity } from 'typeorm';

@Entity()
export class ReporteSistema {
  // Esta entidad está vacía porque solo usamos funciones PostgreSQL
  // Se crea solo para mantener la estructura del módulo
}